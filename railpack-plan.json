{
  "php": "8.4",
  "build": {
    "cmd": "composer install --ignore-platform-reqs --no-scripts && npm install && npm run build"
  },
  "start": {
    "cmd": "php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}