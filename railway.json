{
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "composer install --ignore-platform-reqs --optimize-autoloader --no-interaction && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}